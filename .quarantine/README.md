# Quarantine

Non-MVP artifacts live here until the core loop is ready.

Current MVP focus:

```text
profile → workspace → session → sandbox → event/artifact
```

Move items back only when they are part of the next small build slice.
